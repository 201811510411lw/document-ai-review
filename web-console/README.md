# web-console

营业执照审核结果工作台前端预览版。

当前版本只使用 `src/mocks/` 中的 mock 数据，不接入真实后端 API、企业微信 SDK、OAuth、权限系统或人工复核写回。

## 本地运行

```bash
npm install
npm run dev
```

可预览路由：

- `/reviews`
- `/reviews/blr-20260615-0002`
- `/reviews/blr-20260615-0002/manual-review`
